<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class FlocksSeeder extends Seeder
{
    public function run(): void
    {
        // Flocks are created when batches are added in the app.
        // Buildings are seeded separately; each batch gets its own permanent row.
    }
}
